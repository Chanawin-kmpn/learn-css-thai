---
title: "Introduction"
description: "This is my first post!"
published: true
createdDate: "27-01-2025"
updatedDate: "4-02-2025"
---

## CSS คืออะไร?

CSS (Cascading Style Sheets) คือภาษาที่ใช้สำหรับตกแต่งและจัดรูปแบบการแสดงผลของเว็บไซต์เปรียบเสมือนการ “แต่งตัว” ให้กับโครงสร้าง HTML ของเรา ไม่ว่าจะเป็นการกำหนดสี ขนาด รูปแบบตัวอักษร การจัดวางองค์ประกอบต่างๆ รวมถึงการปรับแต่งเว็บไซต์ให้แสดงผลเหมาะสมบนทุกอุปกรณ์ ทั้งมือถือ แท็บเล็ต และเดสก์ทอป

## ทำไมต้องใช้ CSS?

CSS ทำหน้าที่กำหนดรูปแบบสำหรับเว็บของคุณ รวมถึงการออกแบบ การจัดวางเลย์เอาต์และ การแสดงผลที่แตกต่างกันสำหรับอุปกรณ์และขนาดต่างๆ นอกจากนี้ยังสามารถใช้สำหรับเอฟเฟกต์ต่างๆ เช่น แอนิเมชั่นได้อีกด้วย เพื่อดึงดูดและเพิ่มความน่าสนใจให้กับผู้ใช้หรือผู้เยี่ยมชมเว็บไซต์ของคุณ มาดูเหตุผลสำคัญที่เราจำเป็นต้องใช้ CSS:

### 1. สร้างเว็บไซต์ที่สวยงาม

หากเว็บไซต์มีเพียง HTML อย่างเดียว จะได้เพียงข้อความและรูปภาพที่เรียงต่อกัน ไม่มีการจัดวางที่สวยงาม CSS ช่วยให้เราสามารถ:

- ปรับแต่งสีและรูปแบบตัวอักษร
- จัดวางองค์ประกอบให้น่าสนใจ
- เพิ่มเอฟเฟกต์และการเคลื่อนไหว

<ImageCompare
  firstImage="/images/content/introduction/no-css.png"
  firstImageLabel="เว็บไซต์ที่ไม่ใช้ CSS"
  firstImageAlt="เว็บไซต์ที่ไม่ใช้ CSS"
  secondImage="/images/content/introduction/with-css.png"
  secondImageLabel="เว็บไซต์ที่ใช้ CSS"
  secondImageAlt="เว็บไซต์ที่ใช้ CSS"
/>

### 2. ประหยัดเวลาในการพัฒนา

CSS ช่วยให้เราสามารถกำหนดสไตล์ครั้งเดียวแล้วนำไปใช้ได้ทั้งเว็บไซต์ ไม่ต้องเสียเวลากำหนดสไตล์ซ้ำๆ ในทุกหน้า เช่น:

- กำหนดสีหลักของเว็บไซต์
- รูปแบบปุ่มที่ใช้ทั้งเว็บ
- ขนาดและสไตล์ของหัวข้อ

<Playground  
  id="introduction-1"  
  title="การกำหนดสไตล์ของ CSS เพื่อนำไปใช้ซ้ำ"  
  html={`  
    <div class="container">  
    <!-- หน้าที่ 1 -->  
    <section class="section">  
      <h2 class="site-heading">
        หน้าหลัก
      </h2>  
      <p>เนื้อหาหน้าหลักของเว็บไซต์</p>  
      <button class="site-button">
        อ่านเพิ่มเติม
      </button>  
    </section>

    <!-- หน้าที่ 2 -->
    <section class="section">
      <h2 class="site-heading">
        เกี่ยวกับเรา
      </h2>
      <p>ข้อมูลเกี่ยวกับบริษัทของเรา</p>
      <button class="site-button">
        ติดต่อเรา
      </button>
    </section>

    <!-- หน้าที่ 3 -->
    <section class="section">
      <h2 class="site-heading">บริการ</h2>
      <p>บริการต่างๆ ที่เรามีให้</p>
      <button class="site-button">
        ดูบริการทั้งหมด
      </button>
    </section>

  </div>

`}  
  css={`  
 :root {  
 --primary-color: #84cc16;  
 --secondary-color: #f97316;  
 --text-color: #404040;  
 --bg-color: #f5f5f5;  
 }

    /* รีเซ็ตค่าเริ่มต้น */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    /* สไตล์หลักที่ใช้ทั้งเว็บ */
    body {
      color: var(--text-color);
      font-family: system-ui, sans-serif;
      padding: 2rem;
      background-color: white;
    }

    /* คอนเทนเนอร์หลัก */
    .container {
      max-width: 1000px;
      margin: 0 auto;
    }

    /* สไตล์สำหรับแต่ละเซคชั่น */
    .section {
      background-color: var(--bg-color);
      padding: 1.5rem;
      border-radius: 0.5rem;
      margin-bottom: 2rem;
    }

    /* สไตล์หัวข้อที่ใช้ร่วมกัน */
    .site-heading {
      font-size: 1.875rem;
      font-weight: bold;
      color: var(--primary-color);
      margin-bottom: 1rem;
    }

    /* สไตล์ย่อหน้าที่ใช้ร่วมกัน */
    .section p {
      margin-bottom: 1rem;
      line-height: 1.6;
    }

    /* สไตล์ปุ่มที่ใช้ร่วมกัน */
    .site-button {
      background-color: var(--primary-color);
      color: white;
      padding: 0.5rem 1rem;
      border-radius: 0.375rem;
      font-weight: 500;
      border: none;
      cursor: pointer;
      transition: background-color 0.2s;
    }

    .site-button:hover {
      background-color: var(--secondary-color);
    }

`}  
 mode="default"  
 layoutMode="tabbed"  
 centered={false}  
/>

จากตัวอย่างข้างต้น สามารถเห็นได้ว่าเรากำหนดสไตล์หลักของเว็บไซต์ในตัวแปรที่เรียกว่า Custom Properties หรือ CSS Variables แล้วนำไปใช้ในทุกส่วนของเว็บไซต์ ทำให้การเปลี่ยนแปลงสไตล์เว็บไซต์เป็นเรื่องง่ายและรวดเร็ว

<Callout type="info" title=":root คืออะไร?">
<TextCode>:root</TextCode> คือ pseudo-class ที่ใช้กำหนดตัวแปรที่ใช้ในทั้งหน้าเว็บไซต์ โดยสามารถเรียกใช้ได้ทุกที่ใน CSS โดยใช้ <TextCode>var()</TextCode>ในการเรียกใช้ตัวแปร เช่น <TextCode>var(--primary-color)</TextCode>

ข้อดีของการใช้ <TextCode>:root</TextCode>

- จัดระเบียบ: เก็บตัวแปรไว้ที่เดียว ค้นหาง่าย
- ปรับแต่งง่าย: แก้ค่าที่ <TextCode>:root</TextCode> ทีเดียว เปลี่ยนทั้งเว็บ
- ทำ Theme ได้: สลับ Dark/Light mode ได้ง่าย
- Responsive: ปรับค่าตามขนาดหน้าจอได้

ตัวอย่างการใช้งานจริง:

<CodeBlock index={1}>
{`/* ประกาศใน :root */  
:root {  
    --header-height: 60px;  
    --menu-width: 250px;  
}

    /* ใช้งาน */

.header {  
 height: var(--header-height);  
}

.menu {  
 width: var(--menu-width);  
}

    /* ปรับค่าตาม Media Query */

@media (max-width: 768px) {  
 :root {  
 --header-height: 40px;  
 --menu-width: 100%;  
 }  
} `}

</CodeBlock>

##### เทคนิคการตั้งชื่อตัวแปรใน <TextCode>:root</TextCode> 📝

- ใช้ชื่อที่สื่อความหมาย เช่น <TextCode>--color-primary</TextCode> ดีกว่า <TextCode>--color1</TextCode>
- จัดกลุ่มตัวแปรให้เป็นระบบ เช่น
  - <TextCode>--spacing-...</TextCode> สำหรับระยะห่าง
  - <TextCode>--color-...</TextCode> สำหรับสี
  - <TextCode>--font-...</TextCode> สำหรับฟอนต์

##### ข้อควรระวัง ⚠️

- ตั้งชื่อตัวแปรต้องขึ้นต้นด้วย -- เสมอ
- ค่าที่กำหนดใน <TextCode>:root</TextCode> ควรเป็นค่าพื้นฐานที่ใช้บ่อย
- ระวังการตั้งชื่อซ้ำ เพราะค่าหลังจะทับค่าแรก

</Callout>

### 3. รองรับทุกอุปกรณ์

ปัจจุบันผู้ใช้เข้าเว็บผ่านอุปกรณ์ที่หลากหลาย โดยใน CSS จะเรียกว่า Media Query (เราจะมาทำความรู้จักกันในหัวข้อ <TextLink href="/docs/basics/media-queries">Media Query</TextLink> และเรียนรู้การใช้งานในหัวข้อ <TextLink href="/docs/modern-layout/responsive-design">CSS Responsive</TextLink>) ซึ่ง CSS ช่วยให้:

- เว็บแสดงผลได้ดีทั้งบนมือถือและคอมพิวเตอร์
- ปรับขนาดและการจัดวางตามหน้าจอ
- รักษาประสบการณ์การใช้งานที่ดีบนทุกอุปกรณ์

<CarouselGallery
  images={[
    "/images/content/introduction/desktop-responsive.png",
    "/images/content/introduction/tablet-responsive.png",
    "/images/content/introduction/mobile-responsive.png",
  ]}
/>

## โครงสร้างพื้นฐานของ CSS (Syntax)

### 1. รูปแบบพื้นฐาน

<CodeBlock index={2}>
  {`Selector {
    property: value;
}
`}
</CodeBlock>

ตัวอย่าง:

<CodeBlock copyable={true} index={3}>
  {`p {
    color: red;
    font-size: 16px;
  }
`}
</CodeBlock>

### 2. องค์ประกอบของ CSS

Selector: ตัวเลือกที่ระบุว่าจะจัดรูปแบบให้กับส่วนใดของ HTML

<CodeBlock index={4}>
  {`/\* เลือกทุกย่อหน้า \*/
  p { 
  
  }

/\* เลือกองค์ประกอบที่มี class="button" \*/
.button {

}

/\* เลือกองค์ประกอบที่มี id="header" \*/
#header {

}
`}

</CodeBlock>

Declaration Block: ส่วนที่อยู่ภายในเครื่องหมาย \{ \}

- ประกอบด้วย property และ value
- แต่ละ declaration ต้องจบด้วยเครื่องหมาย Semicolon หรือ <TextCode>;</TextCode>

Property: คุณสมบัติที่ต้องการกำหนด เช่น

<CodeBlock copyable={true} index={5}>
  {`h1 {
    color: red; /\* สีข้อความ \*/
    font-size: 24px; /\* ขนาดตัวอักษร \*/
    margin: 10px; /\* ระยะห่างขอบนอก \*/
    padding: 15px; /\* ระยะห่างของใน \*/
  }
`}
</CodeBlock>

### 3. วิธีการใช้งาน CSS

Inline CSS: เขียนโดยตรงในแท็ก HTML

<CodeBlock language="html" index={6}>
  {`<p style="color: red; font-size: 16px;">Hello, World!</p>
`}
</CodeBlock>

Internal CSS: เขียนในส่วน head ของ HTML

<CodeBlock language="html" index={7}>
  {`<head>
    <style>
      p {
        color: red;
        font-size: 16px;
      }
    </style>
</head>
`}
</CodeBlock>

External CSS: เขียนแยกไฟล์ โดยใช้นามสกุลไฟล์ว่า .css

<CodeBlock language="html" index={8}>
  {`<head>
    <link rel="stylesheet" href="style.css">
</head>
`}
</CodeBlock>

<Callout type="warning" title="สิ่งที่ควรระวัง">
  การเขียน CSS ที่ดีมีหลักการสำคัญที่ควรปฏิบัติตามเพื่อป้องกันข้อผิดพลาดและทำให้เว็บไซต์แสดงผลได้อย่างถูกต้อง เริ่มจากการใส่เครื่องหมาย Semicolon (;) หลังทุก declaration เพื่อแยกคำสั่งแต่ละส่วนให้ชัดเจน 
  
  นอกจากนี้ การสะกดชื่อ property และ value ต้องถูกต้องตามมาตรฐาน CSS เพราะแม้ผิดเพียงตัวอักษรเดียวก็อาจทำให้สไตล์นั้นไม่ทำงาน

เมื่อกำหนดสไตล์ให้กับองค์ประกอบต่างๆ ควรตรวจสอบให้แน่ใจว่าใช้ selector ที่ถูกต้องและตรงกับองค์ประกอบที่ต้องการจัดรูปแบบ พร้อมทั้งคำนึงถึงเรื่อง specificity หรือความเฉพาะเจาะจงของ selector ที่ใช้ เพราะอาจส่งผลต่อการทับซ้อนของสไตล์ได้

</Callout>
