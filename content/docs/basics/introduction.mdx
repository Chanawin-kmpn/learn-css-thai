---
title: "Introduction"
description: "This is my first post!"
date: "27-01-2025"
---

# Introduction CSS

## CSS คืออะไร?

CSS (Cascading Style Sheets) คือภาษาที่ใช้สำหรับตกแต่งและจัดรูปแบบการแสดงผลของเว็บไซต์เปรียบเสมือนการ “แต่งตัว” ให้กับโครงสร้าง HTML ของเรา ไม่ว่าจะเป็นการกำหนดสี ขนาด รูปแบบตัวอักษร การจัดวางองค์ประกอบต่างๆ รวมถึงการปรับแต่งเว็บไซต์ให้แสดงผลเหมาะสมบนทุกอุปกรณ์ ทั้งมือถือ แท็บเล็ต และเดสก์ทอป

## ทำไมต้องใช้ CSS?

CSS ทำหน้าที่กำหนดรูปแบบสำหรับเว็บของคุณ รวมถึงการออกแบบ การจัดวางเลย์เอาต์และ การแสดงผลที่แตกต่างกันสำหรับอุปกรณ์และขนาดต่างๆ นอกจากนี้ยังสามารถใช้สำหรับเอฟเฟกต์ต่างๆ เช่น แอนิเมชั่นได้อีกด้วย เพื่อดึงดูดและเพิ่มความน่าสนใจให้กับผู้ใช้หรือผู้เยี่ยมชมเว็บไซต์ของคุณ มาดูเหตุผลสำคัญที่เราจำเป็นต้องใช้ CSS:

### 1. สร้างเว็บไซต์ที่สวยงาม

หากเว็บไซต์มีเพียง HTML อย่างเดียว จะได้เพียงข้อความและรูปภาพที่เรียงต่อกัน ไม่มีการจัดวางที่สวยงาม CSS ช่วยให้เราสามารถ:

- ปรับแต่งสีและรูปแบบตัวอักษร
- จัดวางองค์ประกอบให้น่าสนใจ
- เพิ่มเอฟเฟกต์และการเคลื่อนไหว

<ImageCompare
  firstImage="/images/content/introduction/with-css.png"
  firstImageLabel="เว็บไซต์ที่ใช้ CSS"
  firstImageAlt="เว็บไซต์ที่ใช้ CSS"
  secondImage="/images/content/introduction/no-css.png"
  secondImageLabel="เว็บไซต์ที่ไม่ใช้ CSS"
  secondImageAlt="เว็บไซต์ที่ไม่ใช้ CSS"
/>

### 2. ประหยัดเวลาในการพัฒนา

CSS ช่วยให้เราสามารถกำหนดสไตล์ครั้งเดียวแล้วนำไปใช้ได้ทั้งเว็บไซต์ ไม่ต้องเสียเวลากำหนดสไตล์ซ้ำๆ ในทุกหน้า เช่น:

- กำหนดสีหลักของเว็บไซต์
- รูปแบบปุ่มที่ใช้ทั้งเว็บ
- ขนาดและสไตล์ของหัวข้อ

### 3. รองรับทุกอุปกรณ์

ปัจจุบันผู้ใช้เข้าเว็บผ่านอุปกรณ์ที่หลากหลาย โดยใน CSS จะเรียกว่า Media Query (เราจะมาลงลึกในหัวข้อ Media Query) ซึ่ง CSS ช่วยให้:

- เว็บแสดงผลได้ดีทั้งบนมือถือและคอมพิวเตอร์
- ปรับขนาดและการจัดวางตามหน้าจอ
- รักษาประสบการณ์การใช้งานที่ดีบนทุกอุปกรณ์

<CarouselGallery
  images={[
    "/images/content/introduction/desktop-responsive.png",
    "/images/content/introduction/tablet-responsive.png",
    "/images/content/introduction/mobile-responsive.png",
  ]}
/>

<Callout type="tip" title="Mobile First">
  การออกแบบเว็บไซต์ในปัจจุบันนิยมใช้แนวคิด "Mobile First"
  คือออกแบบสำหรับมือถือก่อน แล้วค่อยขยายการแสดงผลสำหรับหน้าจอที่ใหญ่ขึ้น
</Callout>

## โครงสร้างพื้นฐานของ CSS (Syntax)

### 1. รูปแบบพื้นฐาน

<CodeBlock>
  {`Selector {
    property: value;
}
`}
</CodeBlock>

ตัวอย่าง:

<CodeBlock copyable={true}>
  {`p {
    color: red;
    font-size: 16px;
  }
`}
</CodeBlock>

### 2. องค์ประกอบของ CSS

Selector: ตัวเลือกที่ระบุว่าจะจัดรูปแบบให้กับส่วนใดของ HTML

<CodeBlock >
  {`/\* เลือกทุกย่อหน้า \*/
  p { 
  
  }

/\* เลือกองค์ประกอบที่มี class="button" \*/
.button {

}

/\* เลือกองค์ประกอบที่มี id="header" \*/
#header {

}
`}

</CodeBlock>

Declaration Block: ส่วนที่อยู่ภายในเครื่องหมาย \{ \}

- ประกอบด้วย property และ value
- แต่ละ declaration ต้องจบด้วยเครื่องหมาย ;

Property: คุณสมบัติที่ต้องการกำหนด เช่น

<CodeBlock copyable={true}>
  {`h1 {
    color: red; /\* สีข้อความ \*/
    font-size: 24px; /\* ขนาดตัวอักษร \*/
    margin: 10px; /\* ระยะห่างขอบนอก \*/
    padding: 15px; /\* ระยะห่างของใน \*/
  }
`}
</CodeBlock>

### 3. วิธีการใช้งาน CSS

Inline CSS: เขียนโดยตรงในแท็ก HTML

<CodeBlock language="html">
  {`<p style="color: red; font-size: 16px;">Hello, World!</p>
`}
</CodeBlock>

Internal CSS: เขียนในส่วน head ของ HTML

<CodeBlock language="html">
  {`<head>
    <style>
      p {
        color: red;
        font-size: 16px;
      }
    </style>
</head>
`}
</CodeBlock>

External CSS: เขียนแยกไฟล์ โดยใช้นามสกุลไฟล์ว่า .css

<CodeBlock language="html">
  {`<head>
    <link rel="stylesheet" href="style.css">
</head>
`}
</CodeBlock>

<Callout type="warning" title="สิ่งที่ควรระวัง">
  การเขียน CSS ควรปฏิบัติตามหลักการดังนี้: <br />
  1. ต้องใส่เครื่องหมาย ; หลังทุก declaration <br />
  2. ระวังการสะกดชื่อ property และ value ให้ถูกต้อง <br />
  3. ตรวจสอบว่าใช้ selector ที่ถูกต้องกับองค์ประกอบที่ต้องการ <br />
  4. ระวังเรื่อง specificity (ความเฉพาะเจาะจง) ของ selector <br />
  <br />
  เพื่อลดความผิดพลาดในการเขียน CSS และให้เว็บไซต์แสดงผลได้ถูกต้อง
  ควรหมั่นตรวจสอบการเขียน CSS อยู่เสมอ
</Callout>
